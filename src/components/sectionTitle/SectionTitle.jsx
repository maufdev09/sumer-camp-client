import React from "react";

const SectionTitle = ({ title }) => {
  return (
    <div>
      <p className="text-3xl py-5 font-semibold text-center">{title}</p>
    </div>
  );
};

export default SectionTitle;
